package com.nettarion.stride.simulator.world;

/** The vanilla fluid tags whose player-motion semantics are in the admitted domain. */
public enum FluidKind {
	/** No fluid. */
	EMPTY,
	/** In {@code FluidTags.WATER}. */
	WATER,
	/** In {@code FluidTags.LAVA}. */
	LAVA
}
